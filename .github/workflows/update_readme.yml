name: Update README

on:
  schedule:
    - cron: '0 */12 * * *' # Cada 12 horas

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Update README with recent activity
        uses: juan1988-tech/juan1988-tech@main # Reemplaza con tu información
  
